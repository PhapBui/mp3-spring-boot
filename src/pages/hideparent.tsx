import * as React from 'react';

interface HideProps {}

const Hide: React.FunctionComponent<HideProps> = (props) => {
  return (
    <>
      <h2>Hide board</h2>
    </>
  );
};

export default Hide;
